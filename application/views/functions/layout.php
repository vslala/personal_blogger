<?php

    if(isset($layout[0]['cover_image']) && $layout[0]['cover_image'] !== '')
        $cover_image_url = $layout[0]['cover_image'];

    if(isset($layout[0]['cover_heading']) && $layout[0]['cover_heading'] !== '')
        $cover_heading = $layout[0]['cover_heading'];

    if(isset($layout[0]['cover_subheading']) && $layout[0]['cover_subheading'] !== '')
        $cover_subheading = $layout[0]['cover_subheading'];


?>